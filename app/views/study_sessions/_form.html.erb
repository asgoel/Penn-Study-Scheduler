<%= form_for [:course, @study_session] do |f| %>
  <% if @study_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study_session.errors.count, "error") %> prohibited this study_session from being saved:</h2>

      <ul>
      <% @study_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :course_id, :value => @study_session.course_id %>
  </div>
  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %><br>
  </div>
  <div class="field">
    <%= f.label :time %>
    <%= f.time_select :time %><br>
  </div>
  <div class="field">
    <%= f.label :location %>
    <%= f.text_field :location %><br><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

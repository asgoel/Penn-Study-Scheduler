<h1><%= current_user.first_name %>'s Schedule</h1>

<table>
  <tr>
    <th>Course</th>
    <th>Options</th>
    <th></th>
  </tr>

<% @registrations.sort_by {|r| r.course.abbrev }.each do |registration| %>
<tr>
    <td><%= registration.course.abbrev %></td>
    <td><%= link_to 'Study', new_course_study_session_path(registration.course) %></td>
    <td><%= link_to 'Destroy', registration, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>
<br>
<a href="/uploads/new">Upload your Schedule</a><br>
<%= link_to 'New Registration', new_registration_path %>
<br><a href="/">Back</a>